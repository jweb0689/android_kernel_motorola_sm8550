# LineageOS Build
name: Build Android 13 (5.15) Kernel with KSU, SUSFS & Boot Repack

on:
  workflow_dispatch:
    inputs:
      kernel_repo:
        description: 'Kernel Repository URL'
        required: true
        default: 'https://github.com/your-username/your-kernel'
      kernel_branch:
        description: 'Kernel Branch'
        required: true
        default: 'main'
      device_defconfig:
        description: 'Defconfig Name (e.g., gki_defconfig)'
        required: true
        default: 'gki_defconfig'
      boot_image_url:
        description: 'Direct Download URL to your stock boot.img or init_boot.img'
        required: true
        default: 'https://link-to-your-stock-image.img'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Build Environment
        uses: actions/checkout@v4

      - name: Setup Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential bc bison ca-certificates curl flex gcc git libc6-dev libssl-dev openssl python3 unzip wget zip zstd
          
      - name: Download ZyC-Clang Toolchain (From GitHub)
        run: |
          echo "Fetching latest ZyC-Clang from official GitHub repository..."
          mkdir clang
          wget -q $(curl -s https://raw.githubusercontent.com/ZyCromerZ/Clang/main/Clang-main-link.txt) -O "zyc-clang.tar.gz"
          tar -xf zyc-clang.tar.gz -C clang
          rm zyc-clang.tar.gz

      - name: Download & Extract Magiskboot (From GitHub)
        run: |
          echo "Extracting native magiskboot binary from official Magisk GitHub release..."
          wget https://github.com/topjohnwu/Magisk/releases/download/v27.0/Magisk-v27.0.apk
          unzip Magisk-v27.0.apk lib/x86_64/libmagiskboot.so
          mv lib/x86_64/libmagiskboot.so magiskboot
          chmod +x magiskboot

      - name: Clone Kernel Source
        run: |
          git clone --depth=1 -b ${{ github.event.inputs.kernel_branch }} ${{ github.event.inputs.kernel_repo }} kernel_workspace

      - name: Integrate KernelSU & SUSFS
        working-directory: kernel_workspace
        run: |
          echo "Setting up KernelSU from GitHub..."
          curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -
          
          echo "Setting up SUSFS from ShirkNeko for Android 13 (5.15) via GitHub..."
          git clone --depth=1 -b gki-android13-5.15 https://github.com/ShirkNeko/susfs4ksu.git ../susfs4ksu
          
          echo "Patching KernelSU..."
          cp ../susfs4ksu/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch KernelSU/
          cd KernelSU
          patch -p1 < 10_enable_susfs_for_ksu.patch
          cd ..
          
          echo "Copying
